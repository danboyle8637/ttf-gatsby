import styled from "styled-components";

export const ImageOverlay = styled.div`
  grid-column: 1 / -1;
  grid-row: 1 / -1;
  background: hsla(227, 30%, 34%, 0.6);
  width: 100%;
  height: 100%;
  z-index: 1;
`;
