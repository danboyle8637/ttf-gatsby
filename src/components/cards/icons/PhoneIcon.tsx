import * as React from "react";
import styled from "styled-components";

import { PhoneCallIcon } from "../../svgs/PhoneCallIcon";

const Icon = styled(PhoneCallIcon)`
  width: 24px;
`;

export const PhoneIcon = () => {
  return <Icon />;
};
